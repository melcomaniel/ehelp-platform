# Optional notes only — Flutter does not load this file.
# Defaults are in lib/config/api_config.dart (Render demo URL).
#
# Override at run time when you need local Nest:
#   flutter run --dart-define=API_BASE_URL=http://127.0.0.1:3001
#   flutter run --dart-define=API_BASE_URL=http://10.0.2.2:3001
#
# Deployed demo:
#   https://<your-api-host>

API_BASE_URL=https://<your-api-host>
