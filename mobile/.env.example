# Optional notes only — Flutter does not load this file.
# Defaults are in lib/config/api_config.dart (local Nest on 127.0.0.1:3001).
#
# Override at run time:
#   flutter run --dart-define=API_BASE_URL=http://127.0.0.1:3001
#   flutter run --dart-define=API_BASE_URL=http://10.0.2.2:3001
#
# Your own hosted API (keep the real host out of version control):
#   flutter run --dart-define=API_BASE_URL=https://<your-api-host>

API_BASE_URL=http://127.0.0.1:3001
