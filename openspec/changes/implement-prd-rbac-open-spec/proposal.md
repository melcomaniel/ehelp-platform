# Proposal: Implement PRD RBAC Policy

Implement the Product Requirements Document RBAC as a canonical policy layer with
explicit role scopes, deny rules, and separation-of-duties checks.

The current regional RBAC matrix remains available for existing admin screens,
but the PRD role model becomes the source of truth for platform, organization,
office, evaluator, approver, and beneficiary authorization decisions.
